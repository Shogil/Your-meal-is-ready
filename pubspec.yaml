name: shopping_cart_workshop
description: A new Flutter project.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2

  # google sign in
  google_sign_in: ^5.0.4
  firebase_auth: ^1.4.1
  firebase_core: ^1.3.0
  cloud_firestore: ^1.0.4


  # state management
  get: ^4.6.1

  # icon of google
  font_awesome_flutter: ^9.2.0

  modal_bottom_sheet: ^2.0.0
  uuid: ^3.0.4
  dio: ^4.0.4

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
    - images/
